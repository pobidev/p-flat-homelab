After battling with Linux permissions for about 2hrs, I've finally gotten it to work. OMV (The NAS) shares its data via NFS,
squashes every IDs to 2000, which has permissions to delete everything inside the media folder. Theres also a new "media" group in the Docker VM, who is going to manage everything related to the media server. I'll probably create different users and groups
to manage different apps or services. Tomorrow, I'll start the migration by creating containers for the Jellyfin stack.